# --- runtime (what the deployed server needs) ---
fastapi>=0.110
uvicorn>=0.29
pydantic>=2
python-multipart>=0.0.9
requests>=2.31
python-dotenv>=1.0
openai>=1.0
gTTS>=2.5
langgraph>=0.2
langchain-core>=0.3

# Optional, heavy extras (NOT needed to run — installed separately if you want them):
#   semantic RAG:        pip install chromadb sentence-transformers
#   streamlit dev UIs:   pip install streamlit streamlit-mic-recorder
#   tests:               pip install pytest
# See requirements-optional.txt
