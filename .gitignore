# --- secrets (NEVER commit your API keys) ---
.env

# --- python ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.chroma/

# --- node ---
node_modules/
frontend/node_modules/
# NOTE: frontend/dist is intentionally committed so hosts (Render native Python)
# can serve the built UI without running Node. Rebuild it before committing UI changes.

# --- os / editor ---
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
